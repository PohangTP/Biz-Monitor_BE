# DB 연결 문자열 (사용자/비밀번호/호스트/DB명을 환경에 맞게 변경)
DB_URL=mysql+pymysql://USER:PASSWORD@HOST:3306/DBNAME

# 파일 업로드 저장 경로 (절대경로 또는 main.py 기준 상대경로, 기본값: ./uploads)
# UPLOAD_DIR=./uploads
# 예) 절대경로: UPLOAD_DIR=D:/pms_uploads

# 서버 포트 (start-server.bat에서 사용)
# BACKEND_PORT=8000
# FRONTEND_PORT=5500
